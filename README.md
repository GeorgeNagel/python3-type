Python 3 type hinting playground

## OS X Setup
```bash
$ brew install python3
$ git clone <repo>
$ cd <path-to-repo>
$ virtualenv venv --python=`which python3`
$ source venv/bin/activate
(venv)$ pip install -r requirements.txt
```
